#pragma once

// Build/version info.
//
// CMake defines PROCROGUE_VERSION to the project version string.
// If you build without CMake, it falls back to "dev".

#ifndef PROCROGUE_VERSION
#define PROCROGUE_VERSION "dev"
#endif

#ifndef PROCROGUE_APPNAME
#define PROCROGUE_APPNAME "ProcRogue"
#endif
